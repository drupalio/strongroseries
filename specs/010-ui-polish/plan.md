# 010 — Plan

## Decisions

- Puro CSS/JS vanilla (sin React/Vite) — mantiene spec 009 satisfecha.
- Tema: duplicar tokens shadcn con `.light` override en :root.
- Toast: función `toast(msg, kind)` global; container fixed.
- Skeletons: clase `.skeleton` con animación shimmer.
- Responsive: media query <640px colapsa nav.

## Gates

- [x] baseline verde (83 tests)
- [x] spec.md sin NEEDS CLARIFICATION
